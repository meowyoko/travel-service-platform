# 疗养旅游服务平台一期开发交接文档

> 更新日期：2026-07-10
>
> 当前阶段：管理端和员工端已接入 PostgreSQL API，真实 Session 认证、第一轮双端核心闭环、服务端分页、商品首图上传、员工评价闭环、三类对账导出和酒店模块原型均已跑通；固定 Mock 仅保留为 Seed、业务服务测试和数据一致性基线。

## 1. 开发前先读

项目的业务和开发约束以以下文件为准：

1. [`AGENTS.md`](./AGENTS.md)：Agent 工作方式、开发顺序和禁止事项。
2. [`疗养旅游服务平台需求文档_原型开发版_v0.2.md`](./疗养旅游服务平台需求文档_原型开发版_v0.2.md)：一期完整业务需求。
3. [`需求阶段流程图/疗养旅游服务平台业务流程说明_v0.21.md`](./需求阶段流程图/疗养旅游服务平台业务流程说明_v0.21.md)：业务流程补充说明。

当前已经确认的开发原则：

- 管理端按 PC 后台开发，员工端按移动端 H5 开发，不先做小程序。
- 先保证业务逻辑和核心闭环，再做视觉美化。
- UI 以 `stitch_ui` 中的原型为风格参考，不要求逐像素照搬。
- UI 与业务逻辑冲突时，以业务逻辑为准。
- 平台不做真实线上支付，不做多供应商入驻。
- 双端运行时数据以 PostgreSQL 为准，页面刷新后会从 API 恢复 Session 和最新业务数据。

需求方在 v0.2 之后又确认：

- 管理端使用用户名和密码登录。
- 运营账号分领导账号和职工账号，第一阶段只做页面级权限。
- 个人订单按出行日期和返程日期流转履约状态。
- 酒店作为服务商品的一类独立管理，同时可被疗养旅游产品配置为可选酒店。
- 酒店支持房型、房型图片、按日期额度价格和人工库存。
- 疗养订单可从当前产品配置的可选酒店中选择酒店和房型；订单确认时扣减对应房晚库存。
- 业绩金额以员工最终消耗额度为准。
- 评价审核通过后可在员工端商品详情展示。
- 商品后续需要介绍图片、视频及住宿、疗愈项目、行程安排等分区内容。

## 2. 技术栈与项目结构

项目使用 pnpm workspace：

```text
apps/
  admin/        React + TypeScript + Vite 后台管理端
  employee/     React + TypeScript + Vite 员工移动端 H5
  api/          Fastify + Drizzle + PostgreSQL API

packages/
  domain/       领域模型、状态定义、订单商品快照、评价
  contracts/    双端共享 API 契约
  mock-data/    固定 Mock 数据及一致性校验
  application/  内存业务服务与单元测试基线

scripts/
  verify-mock.ts
  start-admin-dev.ps1
```

根目录关键配置：

- [`package.json`](./package.json)
- [`pnpm-workspace.yaml`](./pnpm-workspace.yaml)
- [`tsconfig.base.json`](./tsconfig.base.json)

### 2.1 当前重点新增能力

最近一轮已经完成的重点是把原型从“前端本地数据流”推进到“可持续开发的后端数据联动”：

- 数据库从固定 Mock 切到 PostgreSQL，双端通过 Fastify API 读取和写入真实业务数据。
- 管理端和员工端登录改为服务端 Session，Cookie 使用 HttpOnly，Token 入库前做哈希。
- 核心业务写入进入后端服务层，订单确认、额度扣减和额度流水写入在同一事务中完成。
- 管理端主要列表使用服务端分页，员工端主要列表使用分批加载。
- 商品首图支持后台上传 JPEG、PNG、WebP，不再要求手填静态文件路径。
- 个人订单支持员工完成后提交一次评价，后台审核后可展示到员工端商品详情。
- 意向、订单对账、额度流水三类 Excel 导出已由 API 端生成，导出使用完整筛选结果，不受当前页分页影响。
- 酒店已作为 `hotel` 类型服务商品接入 PostgreSQL、共享契约、管理端和员工端。
- 疗养旅游产品可配置可选酒店范围，员工提交意向和后台调整订单时只能从该范围选择。
- 酒店房型支持图片、启停、日期范围批量维护额度价格和总库存，并可用月历查看每日剩余。
- 待确认订单可选择、替换或清空结构化酒店住宿；确认订单时按入住日至离店日前一晚校验并扣减库存。
- 管理端部署在 `/admin/` 子路径，生产构建已设置 Vite `base=/admin/`。

如果新窗口继续开发，应默认沿用这套 API + PostgreSQL + 共享契约模式，不要回退到页面内 Mock 状态。

## 3. 设计与业务参考资源

| 资源 | 位置 | 用途 |
|---|---|---|
| 后台 Stitch UI | [`stitch_ui/stitch_ui`](./stitch_ui/stitch_ui) | 后台视觉风格、布局和控件参考 |
| 后台设计规范 | [`stitch_ui/stitch_ui/serenity_admin/DESIGN.md`](./stitch_ui/stitch_ui/serenity_admin/DESIGN.md) | 后台色彩、排版和组件风格参考 |
| 员工端 UI | [`员工端UI`](./员工端UI) | 员工 H5 页面参考 |
| 员工端设计规范 | [`员工端UI/corporate_service_core/DESIGN.md`](./员工端UI/corporate_service_core/DESIGN.md) | 员工端视觉规范 |
| 业务流程图 | [`需求阶段流程图`](./需求阶段流程图) | 额度、意向、个人订单、团体项目等流程参考 |
| 供应商端历史截图 | [`供应商端截图`](./供应商端截图) | 仅作历史业务参考，不代表要开发供应商端 |
| 业务流程交付物 | [`deliverables/business-process-v0.2`](./deliverables/business-process-v0.2) | HTML、PDF、SVG 等流程说明 |

注意：本项目是我方自营平台，不要根据“供应商端截图”扩展供应商入驻、店铺或资质审核功能。

## 4. 当前已经完成的基础层

### 4.1 领域对象

已在 [`packages/domain/src/models.ts`](./packages/domain/src/models.ts) 建立：

- 运营账号 `OperatorAccount`
- 集团 `Group`
- 员工 `Employee`
- 额度账户 `QuotaAccount`
- 服务商品 `ServiceProduct`
- 酒店房型 `HotelRoomType`
- 酒店每日库存 `HotelRoomDailyInventory`
- 个人意向 `PersonalIntent`
- 个人订单 `PersonalOrder`
- 额度流水 `QuotaTransaction`
- 服务评价 `ServiceReview`

旅游类商品字段通过 `TravelProductDetails` 扩展，没有把通用商品模型写死成旅游商品。

团体项目和退款凭证尚未建模。酒店已复用服务商品模型，通过 `hotelDetails`、房型和每日库存表扩展。

### 4.2 状态定义

状态集中在 [`packages/domain/src/status.ts`](./packages/domain/src/status.ts)，包括：

- 集团和员工状态
- 运营账号角色、状态和页面权限
- 商品状态
- 酒店房型状态复用商品状态
- 意向状态
- 个人订单状态
- 额度流水类型
- 评价状态

### 4.3 订单商品快照

订单使用 [`packages/domain/src/product-snapshot.ts`](./packages/domain/src/product-snapshot.ts) 生成独立商品快照。

快照当前保存：

- 商品 ID、名称、类型
- 商品简介
- 商品主图、图库
- 额度参考
- 服务说明、注意事项
- 完整旅游扩展内容

商品后续修改或下架不会改变历史订单中的商品内容。

### 4.4 Mock 数据和校验

固定数据：

- [`packages/mock-data/src/mock-data.ts`](./packages/mock-data/src/mock-data.ts)

一致性校验：

- [`packages/mock-data/src/verify.ts`](./packages/mock-data/src/verify.ts)
- [`scripts/verify-mock.ts`](./scripts/verify-mock.ts)

Mock 数据覆盖了集团、员工、额度、商品、意向、订单及额度流水之间的关联。
当前还包含运营账号和评价数据。

### 4.5 内存仓储和业务服务

内存仓储：

- [`packages/application/src/repository.ts`](./packages/application/src/repository.ts)

业务服务：

- [`packages/application/src/platform-service.ts`](./packages/application/src/platform-service.ts)

当前已实现的业务能力：

- 运营账号创建、编辑、启停和页面权限配置。
- 集团新增、编辑、级联删除。
- 员工单个新增、Excel 批量导入、编辑、删除。
- 员工手机号密码认证。
- 单个和批量额度发放、正负额度调整。
- 个人订单全额或多次部分退回。
- 服务商品新增、编辑、删除、上架、下架。
- 酒店商品新增、详情查看、受限编辑、删除、上架、下架。
- 酒店房型新增、图片上传、启停和日期库存维护。
- 疗养旅游产品配置可选酒店，并在已有业务记录后允许继续调整可选酒店。
- 员工提交和撤销个人意向。
- 后台跟进、关闭意向、意向转待确认订单。
- 待确认订单调整、订单负责人指派、出行日期调整和结构化酒店住宿选择。
- 确认个人订单并扣减额度、生成额度流水。
- 确认个人订单时扣减已选择房型的每日库存。
- 个人订单取消和按北京时间计算履约状态。
- 已完成订单评价提交的业务服务。
- 评价展示或隐藏审核。

当前唯一性约束：

- 运营账号用户名、集团名称、员工手机号、服务商品名称全局唯一。
- 各业务对象 ID 不重复。
- 同一订单只能存在一条评价。

员工编号目前没有唯一性校验。

### 4.6 双端数据联动

管理端和员工端 React 数据上下文已切换到共享 PostgreSQL API：

- 登录态由服务端 HttpOnly Cookie Session 管理，不再使用 `sessionStorage` 模拟身份。
- 管理端写操作成功后重新读取授权范围内的聚合上下文。
- 员工端提交或撤销意向后重新读取本人数据。
- 页面刷新会恢复服务端 Session 和数据库最新状态，不会重置为固定 Mock。
- 双端运行时已移除 `@travel/mock-data` 依赖；Mock 继续作为数据库 Seed 和测试基线。

2026-07-06 已通过浏览器跑通：员工提交意向 → 后台转待确认订单 → 后台确认并扣减额度 → 员工查看新订单、余额和额度流水。

### 4.7 PostgreSQL 后端基础层

后端第一轮基础已建立：

- [`apps/api`](./apps/api)：Fastify API。
- [`apps/api/src/db/schema.ts`](./apps/api/src/db/schema.ts)：一期 PostgreSQL 表结构。
- [`apps/api/drizzle`](./apps/api/drizzle)：正式数据库迁移文件。
- [`apps/api/src/db/seed.ts`](./apps/api/src/db/seed.ts)：从固定 Mock 初始化空数据库，密码写入前使用 `scrypt` 哈希。
- [`packages/contracts`](./packages/contracts)：双端共享的 API 请求和响应契约。
- [`compose.dev.yml`](./compose.dev.yml)：仅供本地开发的 PostgreSQL 18.4。

当前已实现：

- 管理端和员工端独立 Session 登录、恢复和退出接口。
- Session Token 仅通过 HttpOnly Cookie 返回，数据库只保存 Token 哈希。
- 停用账号登录拦截。
- 管理端和员工端 Session 身份隔离。
- PostgreSQL 健康检查。
- 管理端和员工端分页/分批读取接口。
- 管理端集团、员工、额度、商品、意向和订单读取接口。
- 双端按当前登录人和页面权限裁剪的聚合上下文接口。
- 员工端集团可见商品、本人意向、本人订单和本人额度读取接口。
- 集团、员工、运营账号、额度、商品、意向、订单和评价的现有页面写入接口。
- 员工提交和撤销意向、后台跟进和意向转订单接口。
- 订单确认、额度扣减和额度流水使用同一 PostgreSQL 事务。
- 订单和额度账户使用行级锁，并发确认只允许一次成功。
- 页面权限、员工本人数据和集团商品可见范围均在服务端校验。
- 商品首图上传、静态访问和本地持久化目录忽略。
- 员工评价提交、一单一评校验和后台审核展示。
- 酒店商品、房型、每日库存、疗养产品可选酒店、意向偏好酒店和订单酒店快照。
- 订单酒店选择按当前疗养产品的可选酒店配置校验。
- 酒店库存按房晚校验，离店日不占用库存；确认订单时才扣减库存。
- 意向、订单、额度流水 Excel 导出。

管理端与员工端现有页面均已调用这些 API。前端只负责表单交互和展示，身份、权限、业务校验及事务一致性由服务端负责。

后端关键路径：

- [`apps/api/src/app.ts`](./apps/api/src/app.ts)：Fastify 应用注册入口。
- [`apps/api/src/auth`](./apps/api/src/auth)：登录、Session、密码哈希和权限守卫。
- [`apps/api/src/core/context-routes.ts`](./apps/api/src/core/context-routes.ts)：双端聚合上下文接口。
- [`apps/api/src/core/extended-routes.ts`](./apps/api/src/core/extended-routes.ts)：核心业务写入接口。
- [`apps/api/src/core/core-service.ts`](./apps/api/src/core/core-service.ts)：PostgreSQL 业务服务和事务逻辑。
- [`apps/api/src/core/extended-service.ts`](./apps/api/src/core/extended-service.ts)：酒店、订单调整、评价、导出等扩展业务逻辑。
- [`apps/api/src/core/paged-read-model.ts`](./apps/api/src/core/paged-read-model.ts)：服务端分页读模型。
- [`apps/api/src/core/upload-routes.ts`](./apps/api/src/core/upload-routes.ts)：商品图片上传。
- [`apps/api/src/core/export-routes.ts`](./apps/api/src/core/export-routes.ts)：导出接口。
- [`apps/api/src/core/export-service.ts`](./apps/api/src/core/export-service.ts)：Excel 工作簿生成。
- [`packages/contracts/src`](./packages/contracts/src)：双端共享请求和响应契约。

## 5. 当前已经完成的后台页面

路由定义在 [`apps/admin/src/App.tsx`](./apps/admin/src/App.tsx)。

| 路由 | 页面 | 当前能力 |
|---|---|---|
| `/groups` | 集团管理 | 列表、新增、编辑、删除及风险二次确认 |
| `/employees` | 员工管理 | 搜索筛选、单个新增、Excel 批量导入、模板下载、编辑、删除 |
| `/quotas` | 额度管理 | 先选集团、搜索、部门筛选、集团/部门/手动批选、批量额度发放、近期流水 |
| `/products` | 服务商品列表 | 查询筛选、新增、上下架、进入详情 |
| `/products/:productId` | 服务商品详情 | 查看、受限编辑、二次确认、可选酒店配置、删除原因提示 |
| `/hotels` | 酒店管理 | 酒店列表、新增、查看、上下架 |
| `/hotels/:hotelId` | 酒店详情 | 基础信息、受限编辑、删除、上下架、房型和库存管理 |
| `/intents` | 个人意向 | 查询筛选、指派跟进人、跟进、关闭、转待确认订单 |
| `/orders` | 个人订单 | 查询筛选、查看订单、确认待确认订单并扣减额度 |
| `/orders/:orderId` | 订单详情 | 待确认订单完整调整、负责人、状态、额度、流水、评价、退回快捷入口 |
| `/reviews` | 评价管理 | 状态筛选、查看评价、通过展示、隐藏 |
| `/operator-accounts` | 运营账号 | 领导/职工账号、启停、页面权限配置 |

### 5.1 集团管理规则

- 支持编辑和删除。
- 删除已导入员工的集团时，需要输入集团名称二次确认。
- 页面明确提示集团删除会级联清理员工、订单、意向、额度账户和流水等关联数据。
- 删除集团后会同步移除商品的指定集团可见权限。

### 5.2 员工管理规则

- 新增入口分为“单个新增”和“批量导入”。
- 批量导入必须先选择集团。
- Excel 至少包含员工姓名和手机号。
- 页面提供固定格式导入模板下载。
- 员工姓名和手机号不可修改，只能删除后重建。
- 已形成订单的员工不可删除。

### 5.3 额度管理规则

- 必须先选择集团，再管理该集团员工额度。
- 支持按姓名、手机号、工号搜索。
- 支持按部门筛选。
- 支持集团全选、部门全选和手动勾选。
- 批量发放仅允许同一集团员工。
- 每次额度变动必须生成额度流水。

当前已支持：

- 同集团员工批量发放额度。
- 单个员工增加或调减额度，调减后余额不能小于零。
- 个人订单全额、部分及多次部分退回。
- 退回同步更新额度账户、订单累计退回、最终消耗和额度流水。
- 从订单详情跳转额度退回，完成后可返回订单查看结果。

尚未实现退款凭证图片上传；当前退回仅记录原因和内部备注。

### 5.4 服务商品规则

- 已上架商品不可编辑，必须先下架。
- 已上架商品不可删除，必须先下架。
- 只有未上架且从未产生业务记录的商品才可删除。
- 已产生意向或订单的商品不可删除，只能下架。
- 点击不可删除商品时，会展示具体原因。
- 没有意向和订单时，可编辑全部当前表单字段。
- 已产生意向或订单后：
  - 可编辑：主图路径、简介、目的地特色和可选酒店。
  - 修改服务说明、注意事项、服务范围说明时需要二次确认。
  - 商品名称、类型、额度范围、可见范围、目的地、停留天数和适宜月份等不可编辑。
- 已产生业务记录时显示提示：

```text
该商品已产生意向/订单，修改内容仅影响后续展示，不影响历史订单记录。
```

团体项目模块尚未建立。接入团体项目时，必须补上“商品已被团体项目使用则不可删除”的业务层检查。

商品表单已经把适宜月份改为点击选择组件，新增和编辑均支持清空，并移除了重复的“适宜出行时间”文本字段。

疗养旅游产品已支持配置可选酒店：

- 新建和编辑疗养产品时，可勾选后台已有酒店商品。
- 已有业务记录的疗养产品仍允许调整可选酒店，便于新增合作酒店加入老产品。
- 后台订单调整时读取当前产品最新可选酒店，不只读取订单快照中的旧配置。
- 可选酒店只控制该疗养产品能选哪些酒店；酒店详情、房型和库存仍在酒店管理中维护。

当前商品媒体和详情编排仍不完整：

- `gallery` 图库字段已存在于模型和订单快照，但后台没有维护入口。
- 商品主图已支持 JPEG、PNG、WebP 上传和替换；当前存储在 API 本地持久化目录。
- 没有产品介绍图库维护和商品视频字段。
- 没有疗愈项目、行程安排等可排序的独立图文板块。

### 5.5 酒店管理规则

- 酒店是 `hotel` 类型服务商品，有独立管理菜单。
- 酒店列表操作保持简单：查看、上架或下架。
- 酒店详情页展示基础信息，右上角提供编辑、删除、上下架、管理房型。
- 已上架酒店不可编辑，必须先下架。
- 已有意向、订单或被疗养产品绑定为可选酒店的酒店不可删除。
- 可见范围规则与服务商品一致；“指定 n 个集团”可点击查看具体集团。
- 房型在弹窗中管理，不直接展开在酒店列表下方。
- 房型支持名称、床型、可住人数、早餐、面积、说明和图片。
- 房型可启用或停用。
- 库存维护以“可售夜”为单位，选择起止日期范围后批量设置每日额度价格、总库存和是否可售。
- 起止日期可为同一天，用于单独维护某一晚库存；前端会限制起始日期不能晚于截止日期。
- 库存概览可点击打开月历，选择年月后查看该月每日价格和剩余房间数。
- 月历中“余 n”表示该晚剩余房间数，额度价格显示在下一行。

### 5.6 意向和订单规则

- 提交意向不扣额度。
- 意向操作均要求填写跟进人。
- 意向转订单后先进入待确认状态。
- 待确认订单仍可修改实际方案、出行信息和计划扣减额度。
- 待确认订单的完整调整入口位于订单详情，包含往返、住宿、结构化酒店住宿、接送、日期、额度、方案和内部备注。
- 订单列表中的待确认订单只保留“查看”和“确认”操作。
- 确认订单后才扣减额度并生成关联流水。
- 订单保存商品快照和订单专属服务方案。
- 订单酒店住宿保存订单专属快照，包括酒店、房型、入住日、离店日、房晚数和备注。
- 待确认订单可选择、更换或清空酒店住宿。
- 酒店选择范围读取当前疗养产品的最新可选酒店配置。
- 入住日到离店日前一晚逐日校验房型库存；离店日不占用库存。
- 待确认订单保存酒店住宿只校验库存，不占用库存；确认订单时再次校验并扣减。
- 意向跟进人和订单负责人均从启用中的运营账号选择。
- 管理端可取消订单，但取消后不会自动退回额度。
- 订单状态按北京时间和出行/返程日期计算；当前在进入订单列表或详情时同步，不是后台定时任务。
- 订单详情展示商品快照、行程、额度、关联流水和评价。
- 评价管理支持将评价设为员工端展示或隐藏。
- 员工可对本人已完成订单提交一次 1–5 星和 500 字以内的评价。
- 员工端“我的评价”可查看评价内容和待审核、已展示、未展示状态。

### 5.7 对账与数据导出

- 个人意向可按集团、状态、提交日期和关键词导出员工联系信息及意向内容。
- 订单可按集团、确认日期、商品类型和状态导出对账明细。
- 额度流水可按集团、发生日期和流水类型导出。
- Excel 由服务端根据完整筛选结果生成，不受管理端当前分页影响。
- 单次导出最多 50000 条，超过时要求缩小集团或时间范围。
- 导出权限分别沿用意向、订单和额度页面权限。

### 5.8 后台登录与权限

- 已有用户名密码登录页和会话保持。
- 领导账号默认拥有全部页面权限。
- 普通职工账号按页面配置访问范围。
- 权限同时控制菜单和路由访问，目前不做按钮级权限。
- 浏览器运行时使用服务端 Session；数据库密码字段使用 `scrypt` 哈希，Mock 明文密码只用于本地 Seed 和测试。

## 6. UI 实现位置

后台外壳与公共组件：

- [`apps/admin/src/components/AdminShell.tsx`](./apps/admin/src/components/AdminShell.tsx)
- [`apps/admin/src/components/Modal.tsx`](./apps/admin/src/components/Modal.tsx)
- [`apps/admin/src/components/StatusBadge.tsx`](./apps/admin/src/components/StatusBadge.tsx)
- [`apps/admin/src/components/VisibilityGroupsText.tsx`](./apps/admin/src/components/VisibilityGroupsText.tsx)：指定可见集团弹窗。
- [`apps/admin/src/components/HotelRoomManagerModal.tsx`](./apps/admin/src/components/HotelRoomManagerModal.tsx)：酒店房型、库存维护和库存月历。

数据上下文：

- [`apps/admin/src/context/AdminDataContext.tsx`](./apps/admin/src/context/AdminDataContext.tsx)

后台酒店相关页面：

- [`apps/admin/src/pages/HotelsPage.tsx`](./apps/admin/src/pages/HotelsPage.tsx)
- [`apps/admin/src/pages/HotelDetailPage.tsx`](./apps/admin/src/pages/HotelDetailPage.tsx)
- [`apps/admin/src/pages/ProductDetailPage.tsx`](./apps/admin/src/pages/ProductDetailPage.tsx)：疗养产品可选酒店配置。
- [`apps/admin/src/pages/OrderDetailPage.tsx`](./apps/admin/src/pages/OrderDetailPage.tsx)：待确认订单结构化酒店住宿。

样式：

- [`apps/admin/src/styles/design-tokens.css`](./apps/admin/src/styles/design-tokens.css)
- [`apps/admin/src/styles/global.css`](./apps/admin/src/styles/global.css)

当前 UI 约束：

- PC 后台优先。
- 左侧固定导航、顶部栏、内容区卡片和数据表格。
- 控件和卡片使用较小圆角。
- 禁用输入框、下拉框和字段区域使用灰色背景。
- 不要重新套用暖色编辑风或移动端大卡片布局。

员工端公共组件与数据上下文：

- [`apps/employee/src/components/EmployeeSummaryHeader.tsx`](./apps/employee/src/components/EmployeeSummaryHeader.tsx)
- [`apps/employee/src/components/EmployeeBottomNav.tsx`](./apps/employee/src/components/EmployeeBottomNav.tsx)
- [`apps/employee/src/context/EmployeeDataContext.tsx`](./apps/employee/src/context/EmployeeDataContext.tsx)
- [`apps/employee/src/styles/global.css`](./apps/employee/src/styles/global.css)

员工端酒店相关页面：

- [`apps/employee/src/pages/HomePage.tsx`](./apps/employee/src/pages/HomePage.tsx)：疗养/酒店分类切换。
- [`apps/employee/src/pages/ProductDetailPage.tsx`](./apps/employee/src/pages/ProductDetailPage.tsx)：酒店详情和房型展示。
- [`apps/employee/src/pages/IntentSubmissionPage.tsx`](./apps/employee/src/pages/IntentSubmissionPage.tsx)：意向酒店和房型选择。
- [`apps/employee/src/pages/OrderDetailPage.tsx`](./apps/employee/src/pages/OrderDetailPage.tsx)：订单酒店住宿展示。

## 7. 当前还没有完成的内容

### 7.1 管理端待开发

#### 团体项目

- 团体项目领域模型和 Mock 数据。
- 后台创建、编辑、状态维护。
- 按集团选择或导入参与员工。
- 员工参与状态、额度扣减和退回。
- 商品删除时检查团体项目关联。

#### 商品媒体和细化内容

- 产品介绍图片上传和图库维护。
- 商品视频字段、上传和播放。
- 疗愈项目、行程安排等独立图文板块。
- 正式文件存储；当前商品首图已支持上传，但仍保存在 API 本地目录，生产环境应改为对象存储或共享文件存储。

#### 其他反馈

- 额度退回上传机票损失等图片凭证。
- 按产品、集团和年度汇总最终消耗额度、订单量和服务人数。
- 后端定时推进订单履约状态；当前仅在进入订单页面时同步。
- 酒店库存当前只在订单确认时扣减；待确认订单保存酒店住宿不占用库存。
- 酒店库存没有日历式批量编辑，只支持日期范围批量设置和月历查看。

### 7.2 员工 H5 已完成页面

员工端路由定义在 [`apps/employee/src/App.tsx`](./apps/employee/src/App.tsx)。

| 路由 | 页面 | 当前能力 |
|---|---|---|
| `/login` | 员工登录 | 手机号密码登录、停用账号拦截、会话保持 |
| `/` | 员工首页 | 所属集团、可用额度、疗养/酒店分类切换、商品列表、搜索 |
| `/products/:productId` | 商品详情 | 旅游信息、可选酒店、酒店信息、房型、详细说明、公开评价、提交意向入口 |
| `/products/:productId/intent` | 提交意向 | 日期、计划入住/疗养天数、同行人数、交通、意向酒店、意向房型、接送、联系时间、备注、重复项目和行程冲突提醒 |
| `/intents` | 我的意向 | 状态、跟进信息、允许状态下撤销、关联订单 |
| `/orders` | 我的订单 | 状态筛选、行程信息、额度使用 |
| `/orders/:orderId` | 订单详情 | 行程、专属方案、商品快照、额度明细、评价状态 |
| `/reviews` | 我的评价 | 评分、文字内容、审核和展示状态 |
| `/quota` | 我的额度 | 额度总览、类型筛选、流水、关联订单跳转 |
| `/profile` | 我的 | 个人信息、常用入口、修改密码占位、退出确认 |

员工端已实现：

- 员工只能查看所属集团可见的已上架商品。
- 首页上方使用分类 tag 切换疗养服务和酒店商品，酒店不会混在疗养商品列表中。
- 酒店详情复用商品详情路由，展示酒店基础信息、设施政策、房型图片和参考额度。
- 员工只能查看自己的意向、订单、评价和额度流水。
- 提交意向不扣额度。
- 疗养产品提交意向时，意向酒店只显示该产品后台配置的可选酒店。
- 酒店产品提交意向时，意向酒店默认为当前酒店。
- 酒店意向或疗养意向均可选择已启用房型，也可保留“房型待沟通”。
- 出行日期不能早于当天，并受商品适宜月份限制。
- 商品详情进入后自动回到页面顶部；员工端子页面刷新后回到首页。
- 意向表单已有填写内容时，点击顶部返回会确认内容丢失。
- 同项目已有意向或订单、预计日期与现有订单重叠时只提示风险，不写死禁止再次提交。
- 待跟进或沟通中的本人意向可以撤销。
- 额度页不展示内部备注和操作人。
- 公开状态的评价会展示在对应商品详情。
- 已完成订单可提交一次评价，并在“我的评价”查看审核状态。
- 员工端没有退款申请入口，也不能自行修改订单。

员工端仍待开发：

- 团体项目列表和详情。
- 修改密码。
- 员工端按日期实时查房和库存占用展示。
- 商品介绍图、视频和更细化的图文详情。

### 7.3 员工端 UI 规则

- 移动端内容宽度上限约 480px。
- 正文字号以 14px 为基准，辅助文字通常为 10–13px。
- 普通标题通常为 16–20px，页面主标题约 24px。
- 除额度数字等强信息外，避免大字号和过粗字体。
- 首页、意向/订单、额度和“我的”使用统一顶部集团/额度栏。
- 底部主导航统一为：首页、意向/订单、我的。
- 员工端刷新回首页是当前移动端交互约定，不影响应用内正常页面跳转。

不要提前开发线上支付、供应商入驻、发票、合同或实时机酒预订。

## 8. 最新需求反馈逐项状态

| 需求反馈 | 当前状态 | 说明 |
|---|---|---|
| 商品增加介绍图片和视频 | 未完成 | 图库仅有模型字段，后台无维护 UI；视频尚未建模 |
| 按意向状态导出职工信息 | 已完成 | 支持集团、状态、日期和关键词筛选后导出 Excel |
| 订单跟进人选择本公司员工 | 已完成 | 意向跟进人和订单负责人均选择运营账号 |
| 商品按住宿、疗愈、行程等板块展示 | 部分完成 | 员工端已有文字分区，后台无可维护的独立图文板块 |
| 额度退回上传损失凭证 | 未完成 | 当前只有退回原因和内部备注 |
| 合作酒店独立商品和酒店订单 | 已完成原型 | 酒店、房型、房型图、日期价格和人工库存已接入 |
| 订单住宿安排从商品酒店中选择 | 已完成原型 | 待确认订单可从当前疗养产品可选酒店中选择、替换或清空酒店住宿 |
| 疗养产品配置可选酒店 | 已完成 | 新建和编辑均可勾选酒店，已有业务记录后仍允许调整 |
| 酒店库存月历查看 | 已完成 | 管理房型中点击库存概览查看单月库存日历 |
| 产品/公司/年度业绩汇总 | 未完成 | 当前没有统计看板和汇总导出 |
| 订单按行程日期自动完成 | 已完成核心算法 | 进入订单页面时同步，尚无服务端定时任务 |
| 管理端账号与页面权限 | 已完成 | 领导全权限，职工按页面授权 |
| 员工完成后评价并前台展示 | 已完成 | 一单一评、后台审核、我的评价和商品详情展示均已接入 |

## 9. 推荐后续开发顺序

建议新窗口按以下顺序推进：

```text
1. 让需求方体验酒店模块，收集酒店管理、员工端酒店详情和订单选择酒店的反馈
2. 建立团体项目模型、后台管理和员工端只读页面
3. 扩展商品相册和视频
4. 增加退款凭证
5. 实现后端定时推进订单履约状态
6. 最后实现产品/集团/年度业绩汇总
```

运行时数据联动已完成。后续新增页面不得重新引入浏览器内存仓储，应继续通过共享契约接入 API。

酒店模块已经完成可体验原型，下一窗口优先接需求方反馈和修体验问题。团体项目也在一期对象范围内，但可以排在酒店反馈收口之后。

## 10. 本地运行与演示账号

安装依赖：

```powershell
pnpm install
```

先启动 PostgreSQL 和 API：

```powershell
docker compose -f .\compose.dev.yml up -d postgres
Copy-Item .\apps\api\.env.example .\apps\api\.env
pnpm db:migrate
pnpm db:seed
pnpm dev:api
```

`db:seed` 只在空库首次执行；已有数据时会主动拒绝重复初始化。

再分别启动后台和员工端：

```powershell
pnpm dev:admin
pnpm dev:employee
```

默认地址：

```text
管理端：http://127.0.0.1:5173/
员工端：http://127.0.0.1:5174/
API：http://127.0.0.1:3000/
```

### 10.1 服务器演示环境

当前已部署到无域名 IP 演示环境：

```text
员工端：http://47.109.26.156/
管理端：http://47.109.26.156/admin/
API 健康检查：http://47.109.26.156/api/health
```

服务器关键信息：

- 应用目录：`/opt/travel-service-platform`
- API systemd 服务：`travel-api`
- API 监听：`127.0.0.1:3000`
- nginx 对外监听：`80`
- 员工端静态目录：`apps/employee/dist`
- 管理端静态目录：`apps/admin/dist`
- API 运行时上传目录：`apps/api/data/uploads`

部署注意事项：

- 不要同步本地 `apps/api/data` 到服务器，避免覆盖服务器演示数据和上传图片。
- 不要在服务器小规格实例上跑完整 `pnpm build`，之前会把机器打满导致 SSH 和 HTTP 长时间无响应。
- 推荐部署方式：本地构建前端，服务器只解包源码、执行迁移、覆盖前端 `dist`、重启 API。
- 管理端部署在 `/admin/` 子路径，`apps/admin/vite.config.ts` 已在生产构建时设置 `base: "/admin/"`；如果白屏，优先检查 HTML 是否引用 `/admin/assets/...`。
- 前端构建产物可在本地用 `pnpm --filter @travel/admin build` 和 `pnpm --filter @travel/employee build` 生成，再上传 `apps/admin/dist`、`apps/employee/dist`。
- 服务器迁移命令为 `pnpm db:migrate`，不要对已有演示库执行 `pnpm db:seed`。
- 重启 API：`systemctl restart travel-api`。

商品首图通过管理端上传，开发环境默认保存到
`apps/api/data/uploads`（可通过 `UPLOAD_DIR` 调整），并由 API 的
`/uploads/` 路径提供访问。该运行时目录已加入 Git 忽略规则。

管理端集团、员工、商品、意向、订单、评价、额度员工及流水列表使用
PostgreSQL 服务端分页，默认每页 20 条；员工端商品、意向、订单和额度流水
默认每批 10 条并使用“加载更多”。搜索和状态筛选会重新从第一页查询。

管理端演示账号：

```text
领导账号：leader / 123456
普通职工账号：operator / 123456
```

员工端演示账号：

```text
手机号：13900002001
密码：123456
```

这些密码只用于本地开发 Seed，不得用于正式环境。

## 11. 验证方式和当前结果

验证命令：

```powershell
pnpm typecheck
pnpm test
pnpm test:api
pnpm build
pnpm verify:mock
```

截至本交接文档生成时：

- TypeScript 类型检查通过。
- 自动化测试共 44 项，全部通过。
- 管理端和员工端生产构建均通过。
- Mock 数据一致性校验通过。
- PostgreSQL migration、Seed、Session、权限、分页、图片上传、评价并发、三类 Excel 导出、酒店范围校验、核心闭环和并发扣减 API 集成测试共 16 项，全部通过。
- 双端真实登录、Session 刷新恢复和 PostgreSQL 核心业务联动已通过浏览器回归，控制台无错误。
- Mock 当前已包含酒店商品、酒店房型、酒店每日库存、疗养产品可选酒店、意向偏好酒店和订单酒店住宿快照。
- 员工端主要页面、管理端酒店模块和服务器 IP 演示环境已完成基础访问验证。

## 12. 已知事项和风险

1. 本地开发依赖 Docker PostgreSQL 和 API；电脑重启后需要重新确认容器及三个开发服务已启动。
2. 当前页面通过操作后刷新上下文实现联动，尚未使用 WebSocket 或服务端推送；另一端已打开的页面需要刷新或重新进入页面查看最新变化。
3. 自动化 API 已覆盖认证、核心闭环、事务、并发扣减、一单一评和三类导出；集团编辑、批量员工、退回及评价审核仍可继续补充接口用例。
4. 商品首图已支持上传；当前使用 API 本地持久化目录，正式多实例部署前需要改为共享文件存储或对象存储。
5. 商品 `gallery` 字段存在，但后台新增和编辑页面没有维护入口。
6. 订单状态同步依赖进入管理端订单页面，没有后台定时调度。
7. 取消订单不自动退额度是业务设计，不应改成自动退款。
8. [`apps/admin/src/pages/QuotaPage.tsx`](./apps/admin/src/pages/QuotaPage.tsx) 是未使用的旧页面；当前 `/quotas` 使用 `GroupQuotaPage.tsx`。
9. [`apps/employee/src/pages/AccountLandingPage.tsx`](./apps/employee/src/pages/AccountLandingPage.tsx) 是早期未使用页面；当前“我的”使用 `ProfilePage.tsx`。
10. 团体项目尚未建模，商品删除检查仍保留团体项目关联 TODO。
11. 内存业务服务仍作为单元测试基线；新增 PostgreSQL 业务规则时应同步保持测试语义。
12. v0.2 原本写明评价不在前台展示；需求方后续已明确要求展示，当前代码按后续反馈实现。
13. 主列表已改用服务端分页，但聚合上下文仍保留部分关联数据供详情、删除提示和跨对象选择使用；数据规模继续增长时应将这些关联读取进一步改成详情接口和轻量字典接口。
14. 酒店库存当前不做待确认订单占用，只在订单确认时扣减；如果后续需求要求锁房，需要新增库存预占和释放流程。
15. 酒店库存维护以批量日期范围为主，月历仅查看；如运营反馈维护成本仍高，再考虑日历内直接编辑。
16. 服务器暂无域名和 HTTPS；需求方体验阶段暂用 IP 访问，正式展示前应配置域名、HTTPS 和更稳的部署脚本。

## 13. 新窗口启动时建议提供的上下文

可以直接让新窗口先阅读：

```text
AGENTS.md
一期开发交接文档.md
疗养旅游服务平台需求文档_原型开发版_v0.2.md
需求阶段流程图/疗养旅游服务平台业务流程说明_v0.21.md
```

然后优先按第 9 节接需求方反馈。当前后端数据联动、员工评价、导出闭环和酒店模块原型已经完成，不需要新窗口从这几块重新开工。
